# Do not send photo coordinates to third-party geocoding services

GPS coordinates may be read from photo metadata and retained locally, but the human-readable location label will be entered or confirmed by the user. This trades automatic place naming for a strict privacy boundary in which sensitive photo coordinates never leave the device.
