# Keep the album local-first and single-user

The album will persist photos, audio, metadata, and collection state on the current device without accounts or cloud synchronization. This keeps the experience private and self-contained while avoiding backend complexity; cross-device access is explicitly outside the current scope.
